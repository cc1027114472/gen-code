# 发现与决策
<!-- 
  作用：这是该任务的知识库，用来记录你发现和决定的一切。
  原因：上下文窗口有限，这个文件是你的“外部记忆”，持久且容量无限。
  时机：每当有任何发现后都要更新，尤其是在进行了 2 次查看/浏览器/搜索操作之后（2-Action Rule）。
-->

## 需求
<!-- 
  作用：把用户要求拆解成具体需求。
  原因：让需求始终保持可见，避免你忘记自己在构建什么。
  时机：在阶段 1（需求与探索）期间填写。
  示例：
    - 命令行界面
    - 添加任务
    - 列出所有任务
    - 删除任务
    - Python 实现
-->
<!-- 从用户请求中提取 -->
-

## 研究发现
<!-- 
  作用：记录来自网页搜索、文档阅读或探索过程中的关键发现。
  原因：多模态内容（图片、浏览器结果）不会持续保留，必须立刻写下来。
  时机：每进行 2 次查看/浏览器/搜索操作后，都要更新这一节（2-Action Rule）。
  示例：
    - Python 的 argparse 模块支持子命令，适合构建清晰的 CLI
    - JSON 模块可以方便地处理文件持久化
    - 常见模式：python script.py <command> [args]
-->
<!-- 探索过程中的关键发现 -->
-

## 技术决策
<!-- 
  作用：记录你做出的架构和实现选择，以及相应原因。
  原因：你迟早会忘记自己为何选了某种技术或方案，这张表能把这些知识保存下来。
  时机：每当你做出重要技术选择时更新。
  示例：
    | Use JSON for storage | Simple, human-readable, built-in Python support |
    | argparse with subcommands | Clean CLI: python todo.py add "task" |
-->
<!-- 记录带原因的已做决策 -->
| 决策 | 原因 |
|------|------|
|      |      |

## 遇到的问题
<!-- 
  作用：记录你遇到的问题以及解决方式。
  原因：它和 task_plan.md 里的错误记录类似，但更关注较宽泛的问题，而不只是代码错误。
  时机：遇到阻塞或意外挑战时就记录。
  示例：
    | Empty file causes JSONDecodeError | Added explicit empty file check before json.load() |
-->
<!-- 记录问题及其解决方式 -->
| 问题 | 处理方式 |
|------|----------|
|      |          |

## 资源
<!-- 
  作用：记录你发现的有用 URL、文件路径、API 参考和文档链接。
  原因：方便后续快速引用，不要让重要链接淹没在上下文里。
  时机：只要发现有用资源就补充进来。
  示例：
    - Python argparse 文档：https://docs.python.org/3/library/argparse.html
    - 项目结构：src/main.py, src/utils.py
-->
<!-- URL、文件路径、API 参考 -->
-

## 可视化/浏览器发现
<!-- 
  作用：记录你从图片、PDF 或浏览器结果中得到的信息。
  原因：关键，多模态内容不会保留在上下文里，必须立即转成文本。
  时机：在查看图片或浏览器结果后立刻更新，不要等。
  示例：
    - 截图显示登录表单有邮箱和密码字段
    - 浏览器显示 API 返回 JSON，包含 "status" 和 "data" 字段
-->
<!-- 关键：每进行 2 次查看/浏览器操作后就更新 -->
<!-- 多模态内容必须立即转写为文本 -->
-

---
<!-- 
  提醒：2-Action Rule
  每进行 2 次查看/浏览器/搜索操作后，你都必须更新这个文件。
  这样可以避免上下文重置时丢失视觉信息。
-->
*每进行 2 次查看/浏览器/搜索操作后就更新此文件*
*这样可以防止视觉信息丢失*
